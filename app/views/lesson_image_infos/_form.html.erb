<%= form_for(@lesson_image_info) do |f| %>
  <% if @lesson_image_info.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@lesson_image_info.errors.count, "error") %> prohibited this lesson_image_info from being saved:</h2>

      <ul>
      <% @lesson_image_info.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :lesson_id %><br>
    <%= f.number_field :lesson_id %>
  </div>
  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :lesson_image %><br>
    <%= f.text_field :lesson_image %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
